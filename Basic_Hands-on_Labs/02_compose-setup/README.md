# Lab 02: Docker Compose Setup
## Objectives:
- Define services using Compose
- Expose ports and run NGINX

## Instructions:
1. Run:
   ```bash
   docker-compose up -d
   ```
2. Visit `http://localhost:8080`
3. Cleanup:
   ```bash
   docker-compose down
   ```
